# Lcode-service-process 项目需求提示词

你是一名资深产品经理、系统架构师、Python 后端工程师、Electron 桌面端工程师、AI Agent 工程师、DevOps 工程师和 UI/UX 设计师。请基于以下需求，帮助我完整设计并实现一个名为 **Lcode-service-process** 的桌面端服务流程编排 App。

本项目的核心目标是：让用户通过可视化配置一套“服务流程 / 开发流程 / Agent 工作流”，再由 AI 按照流程节点自动完成项目的搭建、开发、测试、部署和交付。

请不要只给概念方案，要尽可能输出可以直接落地的产品设计、技术架构、数据库结构、接口设计、前端页面、后端逻辑、AI 调用流程、节点系统、权限控制、安全策略、异常处理、测试方案、打包部署方案和后续扩展方案。

---

## 一、项目定位

### 1.1 项目名称

Lcode-service-process

### 1.2 产品类型

一个基于 **Python + Electron** 的桌面端 AI 自动化项目流程编排工具。

它类似于：

- Claude Code 的可视化流程版
- AI Agent 工作流编排工具
- 本地项目自动搭建工具
- 可配置的 AI 自动开发流水线
- 面向开发者和非开发者的低代码 / 半自动代码生成平台

### 1.3 核心价值

用户不需要每次手动写复杂提示词，也不需要反复告诉 AI 开发步骤，而是可以：

1. 配置 AI 服务商、API Key、请求地址和模型名称。
2. 创建一个项目流程图。
3. 在流程图中添加不同类型的节点。
4. 按照开发顺序连接节点。
5. 选择全自动执行，或者每个节点执行前后由用户确认。
6. AI 根据流程节点自动完成项目初始化、代码生成、依赖安装、测试、修复、打包、部署等任务。

---

## 二、技术栈要求

### 2.1 桌面端

使用 Electron 实现桌面应用外壳。

要求：

- 支持 macOS，后续可扩展 Windows 和 Linux。
- 支持本地文件读写。
- 支持启动、停止、监控本地命令行进程。
- 支持弹窗确认、任务日志窗口、流程图编辑器。
- 支持应用设置、主题切换、窗口状态保存。

前端技术可选择：

- React + TypeScript，优先推荐；
- 或 Vue + TypeScript，如果项目更适合也可以说明原因。

UI 风格要求：

- 管理后台风格；
- 左侧导航栏；
- 右侧主内容区域；
- 顶部可以有项目名称、运行状态、模型状态、快捷操作；
- 支持浅色 / 深色主题；
- 整体风格要专业、简洁、适合开发工具。

### 2.2 后端

使用 Python 实现本地服务层。

推荐技术：

- FastAPI：提供本地 HTTP API；
- Pydantic：做配置和请求参数校验；
- SQLite：保存本地数据；
- SQLAlchemy 或 SQLModel：管理数据库模型；
- httpx：请求 AI API；
- asyncio：处理异步任务；
- subprocess / pty：执行本地命令；
- websocket：向前端实时推送日志和节点状态。

### 2.3 前后端通信

Electron 前端通过本地接口调用 Python 服务。

需要设计：

- 本地 API 调用方式；
- WebSocket 实时日志推送；
- 任务状态同步；
- 进程启动与关闭机制；
- Python 服务随 Electron 启动和退出；
- 异常崩溃后的恢复机制。

---

## 三、核心功能模块

请完整设计并实现以下模块。

---

## 四、AI 服务配置模块

用户可以在设置页面配置自己的 AI 服务。

### 4.1 基础配置

字段包括：

- 服务商名称，例如 Anthropic、OpenAI、DeepSeek、Moonshot、本地模型、自定义兼容接口；
- API Key；
- Base URL / 请求地址；
- 模型名称；
- 最大上下文长度；
- 最大输出 Token；
- 温度 temperature；
- 超时时间；
- 重试次数；
- 是否启用流式输出；
- 代理配置；
- 默认系统提示词。

### 4.2 多模型配置

需要支持多个模型配置，例如：

- 规划模型；
- 编码模型；
- 审查模型；
- 总结模型；
- 快速模型；
- 本地备用模型。

每个节点可以选择使用哪个模型配置。

### 4.3 安全要求

- API Key 必须加密保存，不能明文保存在普通配置文件中；
- 至少提供本地加密方案；
- 日志中不能输出完整 API Key；
- 请求失败时不能泄露敏感信息；
- 支持测试连接按钮；
- 支持清空密钥。

---

## 五、项目管理模块

### 5.1 项目列表

左侧导航或项目页面展示所有项目。

每个项目至少包含：

- 项目名称；
- 项目描述；
- 项目类型；
- 本地路径；
- 创建时间；
- 更新时间；
- 最近运行状态；
- 使用的流程模板；
- 使用的默认 AI 配置；
- Git 仓库地址，可选。

### 5.2 新建项目

用户点击“新建项目”后：

1. 打开一个可拖动、可缩放的流程编辑窗口；
2. 默认只有“开始节点”和“结束节点”；
3. 用户可以添加系统预设节点；
4. 用户可以添加自定义节点；
5. 用户可以通过连线定义节点执行顺序；
6. 用户可以保存为流程模板；
7. 用户可以直接运行该流程。

### 5.3 项目运行模式

支持两种运行模式：

#### 5.3.1 全自动模式

类似 `claude --dangerously-skip-permissions` 的体验。

AI 可以按照流程节点自动执行：

- 创建文件；
- 修改代码；
- 安装依赖；
- 运行测试；
- 执行构建；
- 启动服务；
- 提交 Git；
- 部署项目。

但必须加入安全边界：

- 删除文件、覆盖重要文件、执行危险命令、提交 Git、推送远程仓库、部署线上环境时，需要有可配置的安全策略；
- 默认不能直接执行高危操作；
- 用户可以在项目设置中开启“信任该项目并允许自动执行高风险操作”；
- 所有操作必须记录日志。

#### 5.3.2 节点确认模式

每到一个节点，系统弹窗向用户说明：

- 当前节点名称；
- AI 准备做什么；
- 会读取哪些文件；
- 会修改哪些文件；
- 会运行哪些命令；
- 预期输出是什么；
- 潜在风险是什么。

用户可以选择：

- 允许执行；
- 拒绝执行；
- 修改该节点需求后再执行；
- 跳过该节点；
- 停止整个流程；
- 切换为全自动模式。

---

## 六、流程图编辑器模块

### 6.1 基本要求

实现一个可视化流程图编辑器。

要求：

- 支持拖拽节点；
- 支持节点连线；
- 支持删除节点；
- 支持复制节点；
- 支持节点分组；
- 支持放大缩小；
- 支持小地图；
- 支持撤销 / 重做；
- 支持自动布局；
- 支持流程校验；
- 支持保存流程草稿；
- 支持导入 / 导出流程 JSON。

可以优先考虑 React Flow 或同类流程图组件。

### 6.2 默认流程

新建项目时默认包含：

1. 开始节点；
2. 结束节点。

用户可以在中间自由添加节点并连线。

### 6.3 流程校验规则

运行前必须校验：

- 是否存在开始节点；
- 是否存在结束节点；
- 是否所有节点都可从开始节点到达；
- 是否有孤立节点；
- 是否有循环依赖；
- 是否有必填配置未填写；
- 是否有未配置模型的 AI 节点；
- 是否有危险节点未开启确认策略；
- 是否有无效路径或无权限路径。

---

## 七、节点系统设计

节点是本项目最核心的能力。请设计一个可扩展的节点体系。

每个节点至少包含：

- 节点 ID；
- 节点类型；
- 节点名称；
- 节点描述；
- 输入参数；
- 输出结果；
- 使用模型；
- 执行模式；
- 超时时间；
- 重试次数；
- 失败处理策略；
- 是否需要用户确认；
- 前置条件；
- 后置校验；
- 日志记录；
- 节点状态；
- 开始时间；
- 结束时间。

### 7.1 系统预设节点

请至少设计以下节点。

#### 7.1.1 开始节点

用于定义项目基础信息：

- 项目名称；
- 项目目标；
- 项目类型；
- 技术栈偏好；
- 输出目录；
- 默认模型；
- 是否启用全自动模式。

#### 7.1.2 需求澄清节点

AI 阅读用户需求后，自动补全不明确的需求，并生成问题列表。

功能：

- 分析需求缺口；
- 生成澄清问题；
- 给出默认建议；
- 用户可回答或采用默认值；
- 输出最终需求文档。

#### 7.1.3 产品设计节点

输出：

- 产品定位；
- 用户角色；
- 功能清单；
- 页面清单；
- 用户流程；
- MVP 范围；
- 后续扩展范围。

#### 7.1.4 技术架构节点

输出：

- 前端技术栈；
- 后端技术栈；
- 数据库方案；
- 本地文件结构；
- 进程通信方案；
- 安全方案；
- 打包部署方案。

#### 7.1.5 UI 图片实例节点

用于给 AI 提供 UI 风格参考。

功能：

- 上传 UI 截图；
- 上传设计稿；
- 上传竞品截图；
- 让 AI 分析视觉风格；
- 生成页面布局说明；
- 生成组件设计规范；
- 后续 UI 代码节点参考该风格。

#### 7.1.6 服务器 Node

用于配置服务器和部署目标。

字段包括：

- 服务器类型，本地、VPS、云服务器、Docker、Kubernetes；
- SSH 主机；
- SSH 端口；
- 用户名；
- 密钥路径；
- 部署目录；
- 域名；
- HTTPS 配置；
- 环境变量；
- 启动命令；
- 健康检查地址。

#### 7.1.7 后端语言 Node

用于选择和配置后端语言。

支持：

- Python / FastAPI；
- Node.js / NestJS / Express；
- Java / Spring Boot；
- Go / Gin；
- Rust / Axum。

本项目默认使用 Python + FastAPI。

#### 7.1.8 前端框架 Node

用于选择前端框架。

支持：

- React + TypeScript；
- Vue + TypeScript；
- Electron；
- Tailwind CSS；
- Ant Design；
- shadcn/ui；
- 自定义 UI 组件。

本项目默认使用 Electron + React + TypeScript。

#### 7.1.9 数据库 Node

用于配置数据库。

支持：

- SQLite，本项目默认；
- PostgreSQL；
- MySQL；
- MongoDB；
- 本地 JSON 文件，作为简单模式。

#### 7.1.10 MCP 配置 Node

用于配置 MCP Server。

功能：

- 添加 MCP Server；
- 设置启动命令；
- 设置环境变量；
- 测试连接；
- 选择哪些流程节点可以使用 MCP；
- 记录 MCP 工具调用日志。

#### 7.1.11 代理配置 Node

用于配置网络代理。

字段包括：

- HTTP_PROXY；
- HTTPS_PROXY；
- NO_PROXY；
- SOCKS5；
- 是否应用到 AI 请求；
- 是否应用到命令行环境；
- 是否应用到 Git。

#### 7.1.12 Git 仓库 Node

用于配置 Git。

功能：

- 初始化 Git 仓库；
- 绑定远程仓库；
- 配置分支；
- 查看变更；
- 自动生成 commit message；
- 可选自动提交；
- 可选自动推送；
- 推送前必须支持用户确认。

#### 7.1.13 代码生成 Node

AI 根据需求和架构生成代码。

要求：

- 生成前先给出文件计划；
- 尽量小步修改；
- 每次修改后记录差异；
- 支持失败重试；
- 支持用户查看变更；
- 支持回滚。

#### 7.1.14 命令执行 Node

用于执行本地 shell 命令。

字段包括：

- 命令内容；
- 工作目录；
- 环境变量；
- 超时时间；
- 是否需要确认；
- 危险等级；
- 成功判断规则；
- 失败处理策略。

危险命令必须拦截或确认，例如：

- 删除文件；
- 清空目录；
- 修改系统配置；
- 安装全局依赖；
- 推送 Git；
- 部署线上服务。

#### 7.1.15 测试 Node

用于运行测试。

功能：

- 运行单元测试；
- 运行集成测试；
- 运行前端测试；
- 运行 lint；
- 运行 typecheck；
- 解析错误日志；
- 自动发起修复节点。

#### 7.1.16 自动修复 Node

当测试或构建失败时：

- 读取错误日志；
- 分析原因；
- 制定修复计划；
- 修改代码；
- 重新运行测试；
- 限制最大循环次数，避免无限修复。

#### 7.1.17 构建打包 Node

功能：

- 构建前端；
- 打包 Electron；
- 打包 Python 服务；
- 生成安装包；
- 输出构建产物路径；
- 记录构建日志。

#### 7.1.18 部署 Node

功能：

- 本地部署；
- Docker 部署；
- 远程服务器部署；
- 上传文件；
- 执行启动命令；
- 健康检查；
- 失败回滚。

#### 7.1.19 文档生成 Node

自动生成：

- README；
- 安装说明；
- 使用说明；
- 开发文档；
- API 文档；
- 节点配置说明；
- 常见问题。

#### 7.1.20 Skill Node

用于封装可复用能力。

例如：

- 代码审查 Skill；
- UI 生成 Skill；
- 数据库建模 Skill；
- API 设计 Skill；
- 测试生成 Skill；
- 部署 Skill；
- 错误修复 Skill。

Skill Node 要支持导入、导出、复用、版本管理。

#### 7.1.21 人工确认 Node

用于强制用户确认某一步。

弹窗展示：

- 当前流程进度；
- AI 已完成内容；
- 下一步计划；
- 风险说明；
- 用户可编辑补充需求。

#### 7.1.22 结束节点

汇总整个流程输出：

- 项目路径；
- 生成文件；
- 执行过的命令；
- 测试结果；
- 构建结果；
- 部署结果；
- 未完成事项；
- 后续建议。

### 7.2 自定义节点

用户可以创建自定义节点。

自定义节点支持：

- 自定义名称；
- 自定义描述；
- 自定义输入表单；
- 自定义提示词模板；
- 自定义模型；
- 自定义命令；
- 自定义成功判断条件；
- 自定义失败处理；
- 是否允许读文件；
- 是否允许写文件；
- 是否允许执行命令；
- 是否需要人工确认。

---

## 八、AI Agent 执行系统

### 8.1 执行流程

AI 执行一个节点时，应该遵循：

1. 读取节点配置；
2. 读取项目上下文；
3. 读取上游节点输出；
4. 生成执行计划；
5. 如果需要确认，则弹窗给用户确认；
6. 执行 AI 请求；
7. 根据 AI 输出调用工具，例如写文件、执行命令、读日志；
8. 保存节点结果；
9. 推送状态到前端；
10. 判断是否进入下一个节点。

### 8.2 上下文管理

需要设计上下文系统，避免提示词越来越长。

要求：

- 项目级上下文；
- 节点级上下文；
- 文件摘要；
- 历史执行摘要；
- 用户补充需求；
- 错误日志摘要；
- 关键决策记录；
- 支持压缩总结；
- 支持按需检索。

### 8.3 工具调用能力

AI 可以使用内部工具：

- 读取文件；
- 写入文件；
- 修改文件；
- 搜索文件；
- 创建目录；
- 执行命令；
- 读取命令输出；
- 启动服务；
- 停止服务；
- 调用 MCP；
- 调用 Git；
- 调用部署脚本。

所有工具调用必须记录日志。

---

## 九、安全与权限策略

这是本项目必须重点考虑的部分。

### 9.1 操作风险分级

将操作分为：

- 低风险：读取文件、生成新文件、运行只读命令；
- 中风险：修改项目文件、安装项目依赖、运行测试；
- 高风险：删除文件、覆盖配置、执行未知脚本、修改系统环境、提交 Git；
- 极高风险：推送远程仓库、部署线上服务、删除目录、执行 sudo 命令。

### 9.2 安全策略

默认策略：

- 低风险可自动执行；
- 中风险根据项目设置决定；
- 高风险必须确认；
- 极高风险必须二次确认。

用户可以在设置中配置：

- 全自动模式允许的最高风险等级；
- 是否允许执行 shell 命令；
- 是否允许删除文件；
- 是否允许访问项目目录外文件；
- 是否允许联网；
- 是否允许 Git push；
- 是否允许部署。

### 9.3 沙箱建议

请设计可扩展的沙箱方案：

- 限制工作目录；
- 命令白名单 / 黑名单；
- 环境变量隔离；
- 文件操作审计；
- 操作回滚；
- Git 快照；
- 执行前 diff 预览。

---

## 十、页面设计

请设计以下页面。

### 10.1 主布局

- 左侧导航栏；
- 右侧内容区域；
- 顶部状态栏；
- 底部任务日志栏，可折叠；
- 支持深色模式。

### 10.2 导航菜单

至少包括：

1. 首页仪表盘；
2. 项目管理；
3. 流程编辑器；
4. 节点库；
5. 运行任务；
6. 执行日志；
7. AI 模型配置；
8. MCP 配置；
9. 代理配置；
10. Git 配置；
11. 服务器配置；
12. 系统设置。

### 10.3 首页仪表盘

展示：

- 项目总数；
- 运行中任务；
- 最近执行失败任务；
- 最近成功构建；
- 当前默认模型；
- API 连接状态；
- 快捷入口。

### 10.4 项目管理页面

展示项目列表，支持：

- 新建项目；
- 打开项目；
- 复制项目；
- 删除项目；
- 搜索项目；
- 按状态筛选；
- 查看最近运行结果。

### 10.5 流程编辑器页面

核心区域是画布。

需要包含：

- 左侧节点面板；
- 中间流程画布；
- 右侧节点属性面板；
- 顶部运行控制栏；
- 底部日志输出栏。

### 10.6 节点库页面

展示：

- 系统节点；
- 自定义节点；
- Skill 节点；
- 节点模板；
- 节点导入导出。

### 10.7 运行任务页面

展示所有流程运行记录：

- 任务名称；
- 所属项目；
- 当前节点；
- 执行状态；
- 进度；
- 开始时间；
- 耗时；
- 操作按钮。

### 10.8 执行日志页面

展示：

- 节点日志；
- AI 请求日志；
- 工具调用日志；
- 命令执行日志；
- 错误日志；
- 可按项目、节点、时间、状态筛选。

### 10.9 设置页面

包括：

- AI 模型配置；
- API Key 管理；
- 代理设置；
- MCP 设置；
- Git 设置；
- 服务器设置；
- 安全策略；
- 主题设置；
- 数据备份与恢复。

---

## 十一、数据库设计

请设计 SQLite 数据库结构，至少包含以下表：

1. app_settings：应用设置；
2. ai_providers：AI 服务商配置；
3. ai_models：模型配置；
4. projects：项目；
5. workflows：流程模板；
6. workflow_nodes：流程节点；
7. workflow_edges：节点连线；
8. workflow_runs：流程运行记录；
9. node_runs：节点运行记录；
10. node_templates：节点模板；
11. skills：Skill 定义；
12. mcp_servers：MCP 配置；
13. proxy_configs：代理配置；
14. git_configs：Git 配置；
15. server_configs：服务器配置；
16. operation_logs：操作日志；
17. ai_request_logs：AI 请求日志；
18. command_logs：命令执行日志；
19. file_changes：文件变更记录；
20. security_policies：安全策略。

每张表请给出：

- 字段名；
- 字段类型；
- 是否必填；
- 默认值；
- 索引建议；
- 关联关系。

---

## 十二、API 设计

请设计本地 FastAPI 接口，至少包括：

### 12.1 AI 配置接口

- 新增 AI 服务商；
- 修改 AI 服务商；
- 删除 AI 服务商；
- 测试连接；
- 获取模型列表；
- 设置默认模型。

### 12.2 项目接口

- 创建项目；
- 获取项目列表；
- 获取项目详情；
- 更新项目；
- 删除项目；
- 打开项目目录。

### 12.3 工作流接口

- 创建流程；
- 保存流程；
- 获取流程详情；
- 校验流程；
- 导入流程；
- 导出流程；
- 复制流程。

### 12.4 节点接口

- 获取节点模板；
- 创建自定义节点；
- 更新节点配置；
- 删除节点；
- 测试节点。

### 12.5 运行接口

- 启动流程；
- 暂停流程；
- 继续流程；
- 停止流程；
- 重试节点；
- 跳过节点；
- 获取运行状态；
- 获取运行日志。

### 12.6 文件与命令接口

- 读取项目文件；
- 写入项目文件；
- 获取文件 diff；
- 回滚文件；
- 执行命令；
- 停止命令；
- 获取命令输出。

### 12.7 WebSocket

用于实时推送：

- 流程状态；
- 节点状态；
- AI 输出；
- 命令日志；
- 错误信息；
- 用户确认请求。

---

## 十三、执行状态设计

### 13.1 流程状态

- draft：草稿；
- ready：待运行；
- running：运行中；
- waiting_confirm：等待用户确认；
- paused：已暂停；
- success：成功；
- failed：失败；
- cancelled：已取消。

### 13.2 节点状态

- pending：等待执行；
- skipped：已跳过；
- running：执行中；
- waiting_confirm：等待确认；
- success：成功；
- failed：失败；
- retrying：重试中；
- cancelled：已取消。

---

## 十四、异常处理

必须考虑：

- AI API 请求失败；
- API Key 无效；
- 模型不存在；
- 网络超时；
- 代理不可用；
- MCP 服务启动失败；
- 命令执行失败；
- 权限不足；
- 文件被占用；
- 流程配置错误；
- 节点执行超时；
- 用户取消执行；
- Python 服务崩溃；
- Electron 前端崩溃；
- 数据库损坏；
- 磁盘空间不足。

每类异常需要：

- 错误提示；
- 日志记录；
- 是否可重试；
- 用户可执行操作；
- 自动恢复策略。

---

## 十五、测试要求

请设计测试方案：

### 15.1 后端测试

- API 测试；
- 数据库模型测试；
- 节点执行测试；
- AI 请求封装测试；
- 安全策略测试；
- 命令执行测试。

### 15.2 前端测试

- 组件测试；
- 流程编辑器测试；
- 设置页面测试；
- WebSocket 状态同步测试。

### 15.3 端到端测试

- 新建项目；
- 配置模型；
- 创建流程；
- 运行流程；
- 用户确认；
- 生成代码；
- 运行测试；
- 查看日志。

---

## 十六、打包与部署

请提供完整方案：

- Electron 应用打包；
- Python 后端打包；
- 前端构建；
- 本地数据库初始化；
- 自动更新方案，可选；
- macOS 安装包；
- Windows 安装包，可作为后续扩展；
- 日志目录；
- 配置目录；
- 用户数据目录。

---

## 十七、MVP 范围

请先实现一个可运行 MVP，不要一开始做得过大。

MVP 必须包含：

1. Electron + React 主界面；
2. Python FastAPI 本地服务；
3. SQLite 数据库；
4. AI 配置页面；
5. 项目管理页面；
6. 简单流程编辑器；
7. 开始节点、AI 任务节点、命令执行节点、人工确认节点、结束节点；
8. 节点连线；
9. 流程运行；
10. 实时日志；
11. 节点确认弹窗；
12. 基础安全策略；
13. 文件读写；
14. 命令执行；
15. 流程运行记录。

高级能力可以作为后续版本实现。

---

## 十八、版本规划

请按版本规划实现。

### V0.1 MVP

- 本地桌面应用可启动；
- 可配置 AI；
- 可创建项目；
- 可编辑简单流程；
- 可运行节点；
- 可查看日志。

### V0.2 节点体系增强

- 增加更多系统节点；
- 支持自定义节点；
- 支持节点模板；
- 支持节点导入导出。

### V0.3 安全与自动化增强

- 完善风险分级；
- 增加 diff 预览；
- 增加回滚；
- 增加 Git 快照。

### V0.4 MCP 与 Skill

- 支持 MCP Server；
- 支持 Skill Node；
- 支持工具调用审计。

### V0.5 部署与打包

- 支持 Electron 打包；
- 支持 Python 服务打包；
- 支持远程部署节点。

---

## 十九、代码实现要求

实现代码时请遵守：

1. 先给出项目目录结构；
2. 先实现 MVP，不要一次性堆太多复杂功能；
3. 每个模块要职责清晰；
4. 前端组件要可复用；
5. 后端服务要分层；
6. 数据模型要可迁移；
7. 所有配置要可持久化；
8. 重要操作要写日志；
9. 错误要有清晰提示；
10. 不要把 API Key 写死；
11. 不要输出伪代码，要尽量给可运行代码；
12. 如果必须简化，请说明哪些是 MVP 简化，哪些后续补充；
13. 每一步实现后要说明如何运行和验证。

---

## 二十、最终交付物

请最终交付：

1. 完整产品需求文档；
2. 技术架构设计；
3. 数据库设计；
4. API 设计；
5. 前端页面设计；
6. 节点系统设计；
7. AI Agent 执行逻辑；
8. 安全策略设计；
9. MVP 实现代码；
10. 测试方案；
11. 打包部署方案；
12. README 使用说明。

---

## 二十一、执行方式

请按下面顺序工作：

1. 先阅读并理解全部需求；
2. 如果有明显缺失，请先列出问题，并给出默认推荐答案；
3. 在用户未补充时，按推荐答案继续；
4. 先输出整体方案；
5. 再输出 MVP 实现计划；
6. 然后开始创建项目文件；
7. 每完成一个阶段都进行验证；
8. 如果出现错误，先分析原因，再修复；
9. 最后输出运行方法和后续开发建议。

如果你是代码执行型 AI，请直接开始实现 MVP；如果你是规划型 AI，请先输出详细实施方案。
