# Lcode-service-process 企业级项目实现提示词

> 目标：让代码执行型 AI 在无人值守情况下，直接完成一个可运行、可验证、可持续扩展的企业级桌面端「服务流程 / AI Agent 工作流编排 App」可用版本；不是只做 Demo，也不是只为企业版打基础，而是按企业级标准直接生产到可交付版本。
>
> 时间要求：以“明天早上用户要看到结果”为目标，优先完成可启动、可使用、可验证、可提交 GitHub 的版本。实现过程中不要因为非关键高级能力卡住主流程；先把企业级主架构、核心功能闭环和可运行版本做出来，再补齐增强能力。
>
> 使用方式：把本文件作为主提示词交给代码执行型 AI。除非遇到无法绕过的外部凭证、账号登录、系统权限或硬件限制，否则不要向用户提问，直接采用本文的默认决策推进实现。

---

## 0. 最高优先级执行指令

你是一名同时具备以下能力的企业级全栈交付团队：

- 资深产品经理
- 系统架构师
- Python 后端工程师
- Electron 桌面端工程师
- React + TypeScript 前端工程师
- AI Agent 工程师
- DevOps / 打包发布工程师
- 安全工程师
- QA 测试工程师
- UI/UX 设计师
- 技术文档工程师

请基于本提示词，直接设计并实现 **Lcode-service-process**。

### 0.1 不要问我，直接默认决策

我希望「明天醒来看到结果」，因此：

1. 不要因为需求细节不完整而停下来问我。
2. 所有不明确的地方都使用企业级默认方案。
3. 如果必须做取舍，优先顺序为：
   - 企业级可用版本优先：不是只做 MVP Demo，而是先让主流程达到可启动、可使用、可验证、可提交的可用状态
   - 架构清晰
   - 安全可控
   - 可测试
   - 可扩展
   - UI 专业
   - 功能完整
4. 如果遇到需要 API Key、远程服务器账号、Apple 开发者证书、GitHub Token 等外部凭证的地方：
   - 实现配置入口；
   - 实现校验逻辑；
   - 使用占位配置；
   - 不要阻塞主流程。
5. 如果某个高级功能一天内无法完整实现：
   - 先实现可运行骨架和接口；
   - 标记 TODO；
   - 保证系统整体能启动、能点击、能保存数据、能执行基础流程。
6. 不要只输出方案，不要只写伪代码。请创建真实项目文件，写真实代码，运行检查并修复明显错误。

### 0.2 最终必须交付

最终必须至少交付一个企业级可用版本，而不是仅交付概念 Demo 或半成品。交付标准是：应用能启动、核心链路能跑通、代码能构建、基础错误已修复、README 能指导运行，并且在完成后提交并推送到指定 GitHub 仓库。

必须至少交付以下内容：

1. 可启动的 Electron + React + TypeScript 桌面端；
2. 可启动的 Python FastAPI 本地服务；
3. SQLite 数据库与初始化迁移；
4. AI 服务配置页面；
5. 项目管理页面；
6. 可视化流程编辑器页面；
7. 节点库页面；
8. 运行任务页面；
9. 执行日志页面；
10. 系统设置页面；
11. 基础节点执行引擎；
12. 文件读写工具；
13. 命令执行工具；
14. WebSocket 实时日志推送；
15. 基础安全策略；
16. README；
17. 开发运行说明；
18. 测试说明；
19. 后续版本规划；
20. Git 远程仓库配置为 `https://github.com/Linyu-H/lcode-service-process`；
21. 完成可用版本后至少执行一次 Git commit；
22. 如果当前环境已有 GitHub 推送权限，则不需要再次询问用户，直接推送到远程仓库；如果没有权限，则完成本地 commit 并在最终报告中明确提示用户执行认证后 push。

---

## 1. 项目定位

### 1.1 项目名称

**Lcode-service-process**

### 1.2 产品定义

一个基于 **Electron + React + TypeScript + Python FastAPI + SQLite** 的桌面端 AI 自动化项目流程编排工具。

它类似于：

- Claude Code 的可视化流程版；
- AI Agent 工作流编排工具；
- 本地项目自动搭建工具；
- 可配置的 AI 自动开发流水线；
- 面向开发者和非开发者的低代码 / 半自动代码生成平台；
- 企业内部研发流程标准化与自动化工具。

### 1.3 核心价值

用户不需要每次手写复杂提示词，也不需要反复告诉 AI 如何开发项目，而是可以：

1. 配置 AI 服务商、API Key、Base URL、模型和默认参数；
2. 创建一个项目；
3. 通过可视化流程图定义开发流程；
4. 在流程图中添加需求分析、产品设计、架构设计、代码生成、命令执行、测试、修复、打包、部署等节点；
5. 设置每个节点的模型、权限、输入输出和确认策略；
6. 选择全自动执行或节点确认执行；
7. 由 AI 按流程自动完成项目初始化、开发、测试、修复、打包和交付；
8. 全程记录日志、AI 请求、工具调用、文件变更和命令输出。

### 1.4 企业级目标

本项目不是一次性 Demo，而是企业级产品雏形。必须考虑：

- 模块化架构；
- 可维护代码结构；
- 数据库迁移能力；
- 权限与安全边界；
- 操作审计；
- 插件化节点系统；
- AI Provider 抽象；
- WebSocket 实时状态；
- 任务可暂停、停止、重试；
- 日志可检索；
- 后续支持团队协作、远程执行、云端同步和企业 SSO。

---

## 2. 默认技术选型

除非项目环境强烈不支持，否则直接使用以下技术栈，不要再询问用户。

### 2.1 桌面端

- Electron
- React
- TypeScript
- Vite
- React Router
- Zustand 或 Redux Toolkit，优先 Zustand
- TanStack Query，可选
- React Flow，用于流程图编辑器
- Tailwind CSS
- shadcn/ui 或 Radix UI 风格组件，如环境不适合则自建轻量组件
- Lucide React 图标
- Monaco Editor 或 CodeMirror，可作为后续增强，MVP 可先使用 textarea

### 2.2 后端

- Python 3.11+
- FastAPI
- Uvicorn
- Pydantic v2
- SQLAlchemy 2.x
- Alembic，可先保留迁移结构，MVP 可用自动建表
- SQLite
- httpx
- websockets / FastAPI WebSocket
- asyncio
- subprocess / asyncio.subprocess
- cryptography，用于本地密钥加密
- python-dotenv

### 2.3 打包与进程管理

- Electron 主进程启动 Python 服务；
- 开发环境下通过脚本分别启动前端、Electron、后端；
- 生产环境后续使用 PyInstaller 打包 Python 服务；
- Electron Builder 打包桌面应用；
- macOS 作为首要支持平台；
- Windows / Linux 作为后续扩展。

### 2.4 默认端口与目录

- 后端默认监听：`127.0.0.1:8765`
- 前端开发端口：`5173`
- 用户数据目录：由 Electron `app.getPath('userData')` 管理
- 本地数据库：`userData/lcode-service-process/app.db`
- 日志目录：`userData/lcode-service-process/logs`
- 工作区默认目录：`~/LcodeProjects`

---

## 3. 产品功能范围

### 3.1 MVP 必须实现

MVP 必须做到「能启动、能配置、能建项目、能画流程、能运行基础节点、能看日志」。

必须包含：

1. Electron 桌面外壳；
2. React 管理后台式主界面；
3. Python FastAPI 本地服务；
4. SQLite 数据持久化；
5. AI 服务商配置 CRUD；
6. 模型配置 CRUD；
7. 项目 CRUD；
8. 工作流 CRUD；
9. React Flow 流程编辑器；
10. 节点属性面板；
11. 节点库；
12. 工作流校验；
13. 工作流运行；
14. 节点运行记录；
15. WebSocket 实时日志；
16. 命令执行节点；
17. AI 任务节点；
18. 人工确认节点；
19. 开始节点；
20. 结束节点；
21. 基础安全风险分级；
22. 文件读写能力；
23. 操作日志；
24. README 与运行说明。

### 3.2 MVP 可以简化但必须预留扩展

以下能力 MVP 可以做成骨架、占位或基础版本，但架构要预留：

- MCP Server 管理；
- Skill Node；
- 远程服务器部署；
- Docker / Kubernetes；
- 自动更新；
- 企业 SSO；
- 团队协作；
- 云端同步；
- 插件市场；
- 完整沙箱隔离；
- 多语言后端生成；
- 高级向量检索上下文。

---

## 4. UI/UX 设计要求

### 4.1 整体风格

采用专业开发工具 + 企业管理后台风格。

关键词：

- 专业
- 简洁
- 高信息密度
- 类 IDE
- 暗色优先，同时支持浅色
- 适合长时间使用
- 操作反馈明确
- 日志可读性强

### 4.2 主布局

必须包含：

1. 左侧导航栏；
2. 顶部状态栏；
3. 右侧主内容区；
4. 底部可折叠日志面板；
5. 全局运行状态提示；
6. API 连接状态提示；
7. 当前默认模型提示；
8. 主题切换按钮。

### 4.3 导航菜单

至少包含：

1. 首页仪表盘；
2. 项目管理；
3. 流程编辑器；
4. 节点库；
5. 运行任务；
6. 执行日志；
7. AI 模型配置；
8. MCP 配置；
9. 代理配置；
10. Git 配置；
11. 服务器配置；
12. 系统设置。

### 4.4 页面要求

#### 4.4.1 首页仪表盘

展示：

- 项目总数；
- 工作流总数；
- 运行中任务；
- 最近失败任务；
- 最近成功任务；
- 当前默认模型；
- API 连接状态；
- 快捷操作：新建项目、新建流程、配置模型、查看日志。

#### 4.4.2 项目管理页面

功能：

- 项目列表；
- 新建项目；
- 编辑项目；
- 删除项目；
- 打开项目目录；
- 搜索；
- 状态筛选；
- 最近运行结果。

项目字段：

- 名称；
- 描述；
- 类型；
- 本地路径；
- Git 地址；
- 默认工作流；
- 默认模型；
- 创建时间；
- 更新时间；
- 最近状态。

#### 4.4.3 流程编辑器页面

布局：

- 左侧节点面板；
- 中间 React Flow 画布；
- 右侧节点属性面板；
- 顶部运行控制栏；
- 底部实时日志栏。

必须支持：

- 添加节点；
- 删除节点；
- 拖拽节点；
- 节点连线；
- 编辑节点配置；
- 保存流程；
- 校验流程；
- 运行流程；
- 停止流程；
- 查看节点状态；
- 导入 / 导出 JSON。

#### 4.4.4 节点库页面

展示：

- 系统节点；
- 自定义节点；
- Skill 节点；
- 节点模板；
- 节点导入导出。

#### 4.4.5 运行任务页面

展示：

- 运行 ID；
- 项目；
- 工作流；
- 当前节点；
- 状态；
- 进度；
- 开始时间；
- 结束时间；
- 耗时；
- 操作：暂停、继续、停止、重试、查看日志。

#### 4.4.6 执行日志页面

支持：

- 操作日志；
- AI 请求日志；
- 工具调用日志；
- 命令执行日志；
- 文件变更日志；
- 错误日志；
- 按项目、工作流、节点、状态、时间筛选；
- 日志详情抽屉；
- 敏感信息脱敏。

#### 4.4.7 AI 模型配置页面

支持：

- 新增服务商；
- 编辑服务商；
- 删除服务商；
- 测试连接；
- 新增模型；
- 设置默认规划模型；
- 设置默认编码模型；
- 设置默认审查模型；
- 设置默认总结模型；
- 设置默认快速模型。

#### 4.4.8 系统设置页面

包括：

- 主题设置；
- 数据目录；
- 日志级别；
- 安全策略；
- 命令执行策略；
- 备份恢复；
- 关于应用。

---

## 5. AI 服务配置模块

### 5.1 服务商配置字段

`ai_providers` 至少包含：

- id
- name
- provider_type：anthropic / openai / deepseek / moonshot / local / custom_openai_compatible
- base_url
- api_key_encrypted
- api_key_masked
- default_headers_json
- proxy_url
- timeout_seconds
- retry_count
- stream_enabled
- is_active
- created_at
- updated_at

### 5.2 模型配置字段

`ai_models` 至少包含：

- id
- provider_id
- name
- display_name
- role：planning / coding / review / summary / fast / fallback / custom
- max_context_tokens
- max_output_tokens
- temperature
- top_p
- default_system_prompt
- is_default
- is_active
- created_at
- updated_at

### 5.3 默认模型建议

如果用户没有配置模型，系统仍要可启动，并展示引导。

默认预置服务商模板：

- Anthropic
- OpenAI
- DeepSeek
- Moonshot
- OpenAI Compatible
- Local Model

不要预置真实 API Key。

### 5.4 API Key 安全

必须做到：

1. API Key 不明文写日志；
2. API Key 不明文返回前端；
3. 前端只显示脱敏版本；
4. 本地数据库中加密保存；
5. MVP 可使用 `cryptography.Fernet` + 本机生成密钥文件；
6. 后续预留 macOS Keychain / Windows Credential Manager / Linux Secret Service。

### 5.5 AI Provider 抽象

后端实现统一接口：

- `AIClient`
- `ChatMessage`
- `AIRequest`
- `AIResponse`
- `AIStreamChunk`

支持：

- 普通请求；
- 流式请求；
- 超时；
- 重试；
- 错误归一化；
- 日志记录；
- token 使用量记录，若服务商返回。

MVP 至少实现 OpenAI Compatible 调用方式，其他 provider 可复用该适配器或留接口。

---

## 6. 项目管理模块

### 6.1 项目字段

`projects` 至少包含：

- id
- name
- description
- project_type
- local_path
- git_url
- default_workflow_id
- default_model_id
- status
- last_run_status
- created_at
- updated_at

### 6.2 功能

必须实现：

- 创建项目；
- 编辑项目；
- 删除项目；
- 查询项目列表；
- 查询项目详情；
- 打开项目目录；
- 自动创建项目目录；
- 校验目录权限；
- 记录操作日志。

---

## 7. 工作流与流程图系统

### 7.1 工作流字段

`workflows` 至少包含：

- id
- project_id
- name
- description
- version
- graph_json
- status
- is_template
- created_at
- updated_at

### 7.2 节点字段

`workflow_nodes` 至少包含：

- id
- workflow_id
- node_key
- node_type
- name
- description
- position_x
- position_y
- config_json
- input_schema_json
- output_schema_json
- model_id
- execution_mode
- timeout_seconds
- retry_count
- failure_strategy
- require_confirmation
- risk_level
- created_at
- updated_at

### 7.3 边字段

`workflow_edges` 至少包含：

- id
- workflow_id
- source_node_key
- target_node_key
- label
- condition_json
- created_at

### 7.4 流程校验规则

运行前必须校验：

1. 必须存在开始节点；
2. 必须存在结束节点；
3. 所有节点必须可从开始节点到达；
4. 不能存在孤立节点；
5. MVP 默认不允许循环；
6. AI 节点必须配置模型，若未配置则使用默认模型；
7. 命令节点必须配置工作目录；
8. 高风险节点必须确认；
9. 路径必须在项目目录或允许目录内；
10. 节点配置必须通过 schema 校验。

### 7.5 导入导出

工作流支持 JSON 导入导出。

导出内容包括：

- workflow meta
- nodes
- edges
- version
- exported_at

导入时需要做 schema 校验和版本兼容。

---

## 8. 节点系统设计

节点是本项目核心能力，必须设计成可扩展注册表模式。

### 8.1 节点基础结构

每个节点至少包含：

- 节点 ID；
- 节点类型；
- 节点名称；
- 节点描述；
- 输入参数；
- 输出结果；
- 使用模型；
- 执行模式；
- 超时时间；
- 重试次数；
- 失败处理策略；
- 是否需要用户确认；
- 风险等级；
- 前置条件；
- 后置校验；
- 日志记录；
- 节点状态；
- 开始时间；
- 结束时间。

### 8.2 MVP 必须实现的系统节点

#### 8.2.1 StartNode 开始节点

功能：定义流程输入与项目基础信息。

配置：

- 项目名称；
- 项目目标；
- 项目类型；
- 技术栈偏好；
- 输出目录；
- 默认模型；
- 是否启用全自动模式。

#### 8.2.2 AITaskNode AI 任务节点

功能：调用 AI 完成一段任务。

配置：

- 任务标题；
- 系统提示词；
- 用户提示词模板；
- 选择模型；
- 是否启用上下文；
- 是否允许读文件；
- 是否允许写文件；
- 是否允许执行命令；
- 输出格式：text / markdown / json；
- 成功判断规则。

MVP 逻辑：

- 读取节点配置；
- 组合项目上下文和上游输出；
- 调用模型；
- 保存 AI 输出；
- 推送日志。

如果未配置 API Key，则节点应失败并给出清晰错误，不影响应用启动。

#### 8.2.3 CommandNode 命令执行节点

配置：

- 命令内容；
- 工作目录；
- 环境变量；
- 超时时间；
- 风险等级；
- 是否需要确认；
- 成功判断规则；
- 失败处理策略。

必须实现：

- 执行命令；
- 实时输出 stdout / stderr；
- 超时终止；
- 退出码记录；
- 危险命令拦截或确认；
- 日志记录。

#### 8.2.4 ManualConfirmNode 人工确认节点

功能：流程暂停，等待用户确认。

展示：

- 当前流程进度；
- AI 已完成内容；
- 下一步计划；
- 风险说明；
- 用户补充输入。

MVP 可通过前端弹窗或运行任务页面操作确认。

#### 8.2.5 EndNode 结束节点

汇总：

- 项目路径；
- 生成文件；
- 执行过的命令；
- 测试结果；
- 构建结果；
- 未完成事项；
- 后续建议。

### 8.3 企业版预留节点

需要在节点库中预留或以模板形式展示：

1. 需求澄清节点；
2. 产品设计节点；
3. 技术架构节点；
4. UI 图片实例节点；
5. 服务器配置节点；
6. 后端语言节点；
7. 前端框架节点；
8. 数据库节点；
9. MCP 配置节点；
10. 代理配置节点；
11. Git 仓库节点；
12. 代码生成节点；
13. 测试节点；
14. 自动修复节点；
15. 构建打包节点；
16. 部署节点；
17. 文档生成节点；
18. Skill 节点；
19. 条件分支节点；
20. 并行执行节点；
21. 循环节点；
22. HTTP 请求节点；
23. 文件操作节点；
24. 数据转换节点。

这些节点 MVP 可以先实现 schema、UI 展示和不可执行占位。

---

## 9. AI Agent 执行系统

### 9.1 执行流程

执行一个工作流时：

1. 创建 `workflow_run`；
2. 校验流程；
3. 解析 DAG；
4. 从开始节点进入；
5. 为每个节点创建 `node_run`；
6. 读取节点配置；
7. 读取项目上下文；
8. 读取上游节点输出；
9. 检查安全策略；
10. 如果需要确认，进入 `waiting_confirm`；
11. 执行节点；
12. 写入日志；
13. 推送 WebSocket 事件；
14. 保存节点输出；
15. 根据执行结果进入下一节点；
16. 全部完成后更新 `workflow_run` 状态。

### 9.2 运行状态

工作流状态：

- draft
- ready
- running
- waiting_confirm
- paused
- success
- failed
- cancelled

节点状态：

- pending
- skipped
- running
- waiting_confirm
- success
- failed
- retrying
- cancelled

### 9.3 上下文管理

MVP 实现基础上下文：

- 项目信息；
- 工作流信息；
- 当前节点配置；
- 上游节点输出；
- 最近日志摘要；
- 用户补充说明。

企业版预留：

- 文件摘要；
- 历史执行摘要；
- 错误日志摘要；
- 关键决策记录；
- 上下文压缩；
- 向量检索；
- RAG。

### 9.4 内部工具

MVP 至少实现：

- 读取文件；
- 写入文件；
- 修改文件；
- 搜索文件；
- 创建目录；
- 执行命令；
- 停止命令；
- 获取命令输出；
- 记录文件变更；
- 记录操作日志。

后续预留：

- Git 工具；
- MCP 工具；
- 部署工具；
- 浏览器工具；
- 截图工具；
- 测试报告解析工具。

---

## 10. 安全与权限策略

这是企业级系统的核心，不能忽略。

### 10.1 风险等级

操作分为：

- low：读取文件、生成新文件、运行只读命令；
- medium：修改项目文件、安装项目依赖、运行测试；
- high：删除文件、覆盖配置、执行未知脚本、提交 Git；
- critical：推送远程仓库、部署线上服务、删除目录、执行 sudo 命令、修改系统配置。

### 10.2 默认策略

默认：

- low：允许自动执行；
- medium：可由项目设置决定；
- high：必须确认；
- critical：必须二次确认；
- 默认禁止访问项目目录外文件；
- 默认禁止 sudo；
- 默认禁止 `rm -rf /`、格式化磁盘、修改系统目录等危险命令；
- 默认禁止输出完整 API Key。

### 10.3 命令安全检查

实现基础危险命令检测：

- `rm -rf`
- `sudo`
- `chmod -R 777`
- `chown -R`
- `mkfs`
- `dd if=`
- `:(){ :|:& };:`
- `git push`
- `git reset --hard`
- `docker system prune`
- 任何目标路径为 `/`、`~`、系统目录的删除命令

危险命令必须：

- 阻止执行，或
- 标记为 high / critical 并要求确认。

### 10.4 审计日志

所有关键操作必须记录：

- 操作人，本地应用可记为 local_user；
- 操作类型；
- 目标资源；
- 参数摘要；
- 风险等级；
- 是否通过；
- 错误信息；
- 时间。

### 10.5 文件变更记录

写文件前后尽量记录：

- 文件路径；
- 操作类型；
- before hash；
- after hash；
- diff 摘要；
- 所属节点；
- 所属运行。

MVP 可先记录路径和内容摘要，后续增强完整 diff 与回滚。

---

## 11. 数据库设计

使用 SQLite。必须实现自动初始化。

至少包含以下表：

1. `app_settings`
2. `ai_providers`
3. `ai_models`
4. `projects`
5. `workflows`
6. `workflow_nodes`
7. `workflow_edges`
8. `workflow_runs`
9. `node_runs`
10. `node_templates`
11. `skills`
12. `mcp_servers`
13. `proxy_configs`
14. `git_configs`
15. `server_configs`
16. `operation_logs`
17. `ai_request_logs`
18. `command_logs`
19. `file_changes`
20. `security_policies`
21. `confirm_requests`

### 11.1 通用字段规范

所有主业务表建议包含：

- `id`: string UUID 或 integer primary key，默认推荐 string UUID；
- `created_at`: datetime；
- `updated_at`: datetime；
- `deleted_at`: datetime nullable，可后续软删除；
- `metadata_json`: JSON text nullable，用于扩展。

SQLite 中 JSON 可先用 TEXT 存储。

### 11.2 索引建议

必须为以下字段建立索引：

- project_id
- workflow_id
- workflow_run_id
- node_run_id
- status
- created_at
- provider_id
- model_id

---

## 12. FastAPI API 设计

API 前缀：`/api/v1`

### 12.1 系统接口

- `GET /health`
- `GET /api/v1/system/info`
- `GET /api/v1/system/settings`
- `PUT /api/v1/system/settings`

### 12.2 AI 配置接口

- `GET /api/v1/ai/providers`
- `POST /api/v1/ai/providers`
- `GET /api/v1/ai/providers/{id}`
- `PUT /api/v1/ai/providers/{id}`
- `DELETE /api/v1/ai/providers/{id}`
- `POST /api/v1/ai/providers/{id}/test`
- `GET /api/v1/ai/models`
- `POST /api/v1/ai/models`
- `PUT /api/v1/ai/models/{id}`
- `DELETE /api/v1/ai/models/{id}`
- `POST /api/v1/ai/models/{id}/set-default`

### 12.3 项目接口

- `GET /api/v1/projects`
- `POST /api/v1/projects`
- `GET /api/v1/projects/{id}`
- `PUT /api/v1/projects/{id}`
- `DELETE /api/v1/projects/{id}`
- `POST /api/v1/projects/{id}/open-directory`

### 12.4 工作流接口

- `GET /api/v1/workflows`
- `POST /api/v1/workflows`
- `GET /api/v1/workflows/{id}`
- `PUT /api/v1/workflows/{id}`
- `DELETE /api/v1/workflows/{id}`
- `POST /api/v1/workflows/{id}/validate`
- `POST /api/v1/workflows/{id}/duplicate`
- `GET /api/v1/workflows/{id}/export`
- `POST /api/v1/workflows/import`

### 12.5 节点接口

- `GET /api/v1/node-templates`
- `POST /api/v1/node-templates`
- `PUT /api/v1/node-templates/{id}`
- `DELETE /api/v1/node-templates/{id}`
- `POST /api/v1/nodes/test`

### 12.6 运行接口

- `GET /api/v1/runs`
- `POST /api/v1/workflows/{id}/run`
- `GET /api/v1/runs/{id}`
- `POST /api/v1/runs/{id}/pause`
- `POST /api/v1/runs/{id}/resume`
- `POST /api/v1/runs/{id}/stop`
- `POST /api/v1/runs/{id}/retry-node/{node_run_id}`
- `POST /api/v1/runs/{id}/skip-node/{node_run_id}`
- `POST /api/v1/confirm-requests/{id}/approve`
- `POST /api/v1/confirm-requests/{id}/reject`

### 12.7 文件与命令接口

- `GET /api/v1/files/read`
- `POST /api/v1/files/write`
- `POST /api/v1/files/diff`
- `POST /api/v1/files/rollback`
- `POST /api/v1/commands/run`
- `POST /api/v1/commands/{id}/stop`
- `GET /api/v1/commands/{id}/logs`

### 12.8 日志接口

- `GET /api/v1/logs/operations`
- `GET /api/v1/logs/ai-requests`
- `GET /api/v1/logs/commands`
- `GET /api/v1/logs/file-changes`

### 12.9 WebSocket

- `WS /ws/runs/{run_id}`
- `WS /ws/events`

事件类型：

- `workflow.started`
- `workflow.status_changed`
- `workflow.completed`
- `workflow.failed`
- `node.started`
- `node.log`
- `node.output`
- `node.waiting_confirm`
- `node.completed`
- `node.failed`
- `command.output`
- `ai.stream`
- `confirm.requested`
- `error`

---

## 13. 后端架构要求

推荐目录：

```text
backend/
  app/
    main.py
    core/
      config.py
      database.py
      security.py
      logging.py
      encryption.py
      paths.py
    models/
      base.py
      ai.py
      project.py
      workflow.py
      run.py
      logs.py
      settings.py
    schemas/
      ai.py
      project.py
      workflow.py
      run.py
      logs.py
    api/
      deps.py
      v1/
        router.py
        ai.py
        projects.py
        workflows.py
        runs.py
        nodes.py
        files.py
        commands.py
        logs.py
        system.py
    services/
      ai/
        base.py
        openai_compatible.py
        manager.py
      workflow_engine.py
      node_registry.py
      security_policy.py
      command_runner.py
      file_service.py
      log_service.py
      websocket_manager.py
      confirmation_service.py
    nodes/
      base.py
      start.py
      ai_task.py
      command.py
      manual_confirm.py
      end.py
    utils/
      ids.py
      time.py
      json.py
  tests/
  requirements.txt
  pyproject.toml
```

要求：

- API 层只处理请求响应；
- Service 层处理业务逻辑；
- Node 层处理节点执行；
- Model 层只定义数据库；
- Schema 层只定义 Pydantic DTO；
- 安全策略独立模块；
- WebSocket 独立管理；
- 所有异常统一返回结构。

---

## 14. 前端架构要求

推荐目录：

```text
frontend/
  src/
    main.tsx
    App.tsx
    routes/
      index.tsx
    layouts/
      AppLayout.tsx
    components/
      ui/
      common/
      workflow/
      logs/
    pages/
      DashboardPage.tsx
      ProjectsPage.tsx
      WorkflowEditorPage.tsx
      NodeLibraryPage.tsx
      RunsPage.tsx
      LogsPage.tsx
      AISettingsPage.tsx
      McpSettingsPage.tsx
      ProxySettingsPage.tsx
      GitSettingsPage.tsx
      ServerSettingsPage.tsx
      SystemSettingsPage.tsx
    services/
      api.ts
      websocket.ts
      ai.ts
      projects.ts
      workflows.ts
      runs.ts
      logs.ts
    stores/
      appStore.ts
      workflowStore.ts
      runStore.ts
    types/
      ai.ts
      project.ts
      workflow.ts
      run.ts
      logs.ts
    styles/
      globals.css
  package.json
  vite.config.ts
  tsconfig.json
```

Electron 目录：

```text
electron/
  main.ts
  preload.ts
  tsconfig.json
```

根目录：

```text
package.json
README.md
.env.example
.gitignore
任务.md
```

### 14.1 前端实现要求

必须：

- 有完整路由；
- 有主布局；
- 有导航；
- 有 API 封装；
- 有类型定义；
- 有 loading / error 状态；
- 有基础表单校验；
- 有可复用 UI 组件；
- 流程编辑器可拖拽节点和连线；
- 节点配置可编辑并保存；
- 运行流程后能看到状态和日志。

---

## 15. Electron 要求

Electron 主进程负责：

1. 创建主窗口；
2. 管理窗口状态；
3. 启动 Python 后端服务；
4. 监听后端健康检查；
5. 应用退出时关闭 Python 服务；
6. 暴露安全 preload API；
7. 禁止 renderer 直接访问 Node.js 全能力；
8. 支持打开本地目录；
9. 后续支持自动更新。

安全要求：

- `contextIsolation: true`
- `nodeIntegration: false`
- 使用 preload 暴露有限 API
- 外部链接用系统浏览器打开
- 不允许任意远程代码注入

---

## 16. 测试与验证要求

### 16.1 后端验证

至少运行或提供：

- `python -m compileall backend/app`
- FastAPI 启动验证；
- `/health` 接口验证；
- 数据库初始化验证；
- 基础 API CRUD 验证；
- 命令安全检测验证。

### 16.2 前端验证

至少运行或提供：

- `npm install` 或对应包管理器安装；
- `npm run typecheck`；
- `npm run build`；
- 页面可启动；
- 流程编辑器可渲染。

### 16.3 端到端手动验证路径

必须保证以下路径可操作：

1. 启动后端；
2. 启动前端 / Electron；
3. 打开首页；
4. 新建 AI Provider；
5. 新建项目；
6. 打开流程编辑器；
7. 添加开始节点、AI 任务节点、命令节点、结束节点；
8. 连线；
9. 保存流程；
10. 运行流程；
11. 查看实时日志；
12. 查看运行记录。

如果 AI API Key 未配置，AI 节点应显示明确错误，但应用不能崩溃。

---

## 17. 异常处理要求

必须考虑并给出实现：

- AI API 请求失败；
- API Key 无效；
- 模型不存在；
- 网络超时；
- 代理不可用；
- MCP 服务启动失败；
- 命令执行失败；
- 命令超时；
- 权限不足；
- 文件被占用；
- 流程配置错误；
- 节点执行超时；
- 用户取消执行；
- Python 服务崩溃；
- Electron 前端崩溃；
- 数据库损坏；
- 磁盘空间不足。

每类异常至少要有：

- 用户可读错误信息；
- 技术日志；
- 是否可重试；
- 建议操作；
- 不让整个应用无提示崩溃。

---

## 18. 打包部署方案

MVP 阶段至少提供文档和脚本雏形：

- 后端开发启动脚本；
- 前端开发启动脚本；
- Electron 开发启动脚本；
- 前端构建脚本；
- Electron 构建脚本；
- Python 后端 PyInstaller 打包预留；
- macOS 打包说明；
- Windows / Linux 后续说明。

---

## 19. README 必须包含

README 至少包括：

1. 项目介绍；
2. 功能特性；
3. 技术栈；
4. 目录结构；
5. 环境要求；
6. 安装依赖；
7. 启动后端；
8. 启动前端；
9. 启动 Electron；
10. 配置 AI Provider；
11. 创建项目和流程；
12. 运行流程；
13. 安全策略说明；
14. 常见问题；
15. 后续路线图。

---

## 20. 版本规划

### V0.1 MVP

目标：可本地启动、可配置 AI、可创建项目、可编辑流程、可运行基础节点、可查看日志。

功能：

- Electron + React 主界面；
- FastAPI 本地服务；
- SQLite；
- AI 配置；
- 项目管理；
- 工作流编辑器；
- Start / AI Task / Command / Manual Confirm / End 节点；
- WebSocket 日志；
- 基础安全策略；
- README。

### V0.2 节点体系增强

- 更多系统节点；
- 自定义节点；
- 节点模板；
- 节点导入导出；
- 条件分支；
- 并行执行。

### V0.3 安全与自动化增强

- diff 预览；
- 文件回滚；
- Git 快照；
- 更强命令沙箱；
- 操作审批策略。

### V0.4 MCP 与 Skill

- MCP Server 管理；
- Skill Node；
- 工具调用审计；
- 外部工具权限管理。

### V0.5 打包与部署

- Electron Builder；
- PyInstaller；
- macOS 安装包；
- 自动更新预留；
- 远程部署节点。

### V1.0 企业版

- 团队协作；
- 企业权限；
- 项目模板市场；
- 审计报表；
- 云同步；
- SSO；
- 私有化部署；
- 插件生态。

---

## 21. 实现顺序

请严格按以下顺序推进，不要卡在过度设计上。目标是明天早上交付一个企业级可用版本并完成一次 Git 提交 / 推送：

1. 检查当前仓库结构；
2. 确认或设置 Git 远程仓库为 `https://github.com/Linyu-H/lcode-service-process`；
3. 创建项目目录结构；
4. 初始化后端 FastAPI；
5. 初始化数据库模型；
6. 实现系统健康检查；
7. 实现 AI Provider / Model CRUD；
8. 实现项目 CRUD；
9. 实现工作流 CRUD；
10. 实现节点模板；
11. 实现运行记录；
12. 实现 WebSocket 管理；
13. 实现安全策略；
14. 实现命令执行器；
15. 实现节点注册表和 MVP 节点；
16. 实现工作流执行引擎；
17. 初始化前端 Vite + React；
18. 实现主布局和路由；
19. 实现 Dashboard；
20. 实现 AI 设置页；
21. 实现项目管理页；
22. 实现流程编辑器；
23. 实现节点库；
24. 实现运行任务页；
25. 实现日志页；
26. 实现 Electron 主进程；
27. 联调前后端；
28. 补齐 README；
29. 运行 typecheck / build / compile；
30. 修复错误；
31. 执行 `git status` 检查变更；
32. 执行一次 Git commit，提交信息应描述当前可用版本；
33. 如果当前环境已有 GitHub 认证与推送权限，不需要再次询问用户，直接执行 `git push` 推送到 `https://github.com/Linyu-H/lcode-service-process`；
34. 如果没有推送权限，不要卡住实现，保留本地 commit，并在最终报告中明确说明需要用户认证后 push；
35. 输出最终交付总结。

---

## 22. 代码质量要求

必须遵守：

1. 代码结构清晰；
2. 命名语义明确；
3. 不写无意义大段注释；
4. 不硬编码 API Key；
5. 不把敏感信息写入日志；
6. 前后端类型尽量对齐；
7. API 返回结构统一；
8. 错误处理明确；
9. 重要操作有日志；
10. 文件修改尽量小步；
11. 可运行优先于空泛完美；
12. 不要留下无法启动的半成品；
13. 如果环境限制导致某些命令无法运行，要在最终报告中明确说明。

---

## 23. 默认业务决策

如果没有用户补充，采用以下默认值：

- 交付目标：直接生产企业级可用版本，不以 Demo 为目标；

- 前端框架：React + TypeScript；
- UI 风格：深色专业开发工具风格，支持浅色；
- 流程图：React Flow；
- 后端：FastAPI；
- ORM：SQLAlchemy；
- 数据库：SQLite；
- AI 调用：优先 OpenAI Compatible 抽象；
- API Key 加密：Fernet 本地加密；
- 项目默认目录：`~/LcodeProjects`；
- 风险策略：高风险必须确认；
- 真实远程部署默认先做配置、校验、日志和接口骨架，除非用户提供服务器凭证；
- 云端账号系统默认先预留架构，本地单用户模式先完整可用；
- 本地单用户应用先完整可用，同时数据库和服务层预留团队 / 企业字段；
- 复杂权限角色默认先实现安全策略与操作风险控制，企业 RBAC 预留表结构和接口边界；
- 工作流默认不允许循环，循环节点作为后续增强；
- 并行节点作为后续增强。

---

## 24. 最终输出格式

完成后，请输出：

1. 已完成内容清单；
2. 关键目录结构；
3. 后端启动命令；
4. 前端启动命令；
5. Electron 启动命令；
6. 测试 / 构建结果；
7. 已知限制；
8. 明天用户打开后应该怎么验证；
9. 后续建议优先级。

如果有失败项，必须如实说明：

- 哪个命令失败；
- 失败原因；
- 已尝试修复什么；
- 用户下一步如何处理。

---

## 25. 立即开始

如果你是代码执行型 AI：

- 可以调研一下最新的别的大神的思路；
- 不要再输出长篇空方案后停止；
- 请直接开始创建和修改项目文件；
- 先实现 V0.1 MVP；
- 每完成关键阶段进行验证；
- 遇到缺失信息使用默认决策；
- 尽可能在无人值守情况下推进到可运行状态。

如果你是规划型 AI：

- 输出完整企业级实现方案；
- 输出任务拆解；
- 输出文件结构；
- 输出接口和数据库设计；
- 然后提示代码执行型 AI 按本提示词继续实现。
