import { api } from './api'; import type { NodeTemplate, Workflow } from '../types/workflow';
export const workflowService = { list: () => api.get<Workflow[]>('/api/v1/workflows'), create: (w: Partial<Workflow>) => api.post<Workflow>('/api/v1/workflows', w), update: (id: string, w: Partial<Workflow>) => api.put<Workflow>(`/api/v1/workflows/${id}`, w), validate: (id: string) => api.post<{valid:boolean;errors:string[];warnings:string[]}>(`/api/v1/workflows/${id}/validate`), run: (id: string) => api.post(`/api/v1/workflows/${id}/run`), templates: () => api.get<NodeTemplate[]>('/api/v1/node-templates') };
