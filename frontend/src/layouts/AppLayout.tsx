import { NavLink, Outlet } from 'react-router-dom';
import { Activity, Boxes, Brain, FolderKanban, GitBranch, Home, ListTree, Network, PlayCircle, ScrollText, Server, Settings, Shield, SlidersHorizontal } from 'lucide-react';
import { useEffect } from 'react';
import { API_BASE } from '../services/api';
import { useAppStore } from '../stores/appStore';
import { BottomLogPanel } from '../components/logs/BottomLogPanel';
const nav=[['/','首页',Home],['/projects','项目管理',FolderKanban],['/workflow','流程编辑器',GitBranch],['/nodes','节点库',Boxes],['/runs','运行任务',PlayCircle],['/logs','执行日志',ScrollText],['/ai','AI 模型配置',Brain],['/mcp','MCP 配置',Network],['/proxy','代理配置',SlidersHorizontal],['/git','Git 配置',GitBranch],['/servers','服务器配置',Server],['/settings','系统设置',Settings]] as const;
export function AppLayout(){ const apiOnline=useAppStore(s=>s.apiOnline); const setApiOnline=useAppStore(s=>s.setApiOnline); const theme=useAppStore(s=>s.theme); const setTheme=useAppStore(s=>s.setTheme); useEffect(()=>{ document.documentElement.dataset.theme=theme; const t=setInterval(()=>fetch(`${API_BASE}/health`).then(r=>setApiOnline(r.ok)).catch(()=>setApiOnline(false)),3000); return()=>clearInterval(t);},[theme,setApiOnline]); return <div className="app-shell"><aside className="sidebar"><div className="brand"><Shield/><div><strong>Lcode</strong><span>Service Process</span></div></div><nav>{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'}><Icon size={18}/><span>{label}</span></NavLink>)}</nav></aside><div className="workspace"><header className="topbar"><div><strong>企业级 AI Agent 工作流编排</strong><span className="muted">默认模型：未配置 · 本地服务 127.0.0.1:8765</span></div><div className="top-actions"><span className={`status-dot ${apiOnline?'online':'offline'}`}><Activity size={14}/>{apiOnline?'API 在线':'API 离线'}</span><button className="btn btn-secondary" onClick={()=>setTheme(theme==='dark'?'light':'dark')}>{theme==='dark'?'浅色':'深色'}</button></div></header><main id="main"><Outlet /></main><BottomLogPanel /></div></div> }
