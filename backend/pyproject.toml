[project]
name = "lcode-service-process-backend"
version = "0.1.0"
description = "Local FastAPI backend for Lcode Service Process"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
